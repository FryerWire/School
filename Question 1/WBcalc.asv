
clear, clc

while true
    WB_Call = WBcalculator()

    user_command = input("\nWould you like to run the program again? (Enter 'Yes' or 'No'): ")
    if ~ ((user_command == "Yes") || (user_command == "No"))
        fprintf("ERROR: Please either enter 'Yes' or 'No")
        continue
    elseif (user_command == "no"):
        fprintf("\nThank you for using the '1965 Piper Cherokee PA-28-180' Flight Approval System")
        break
    end
end
